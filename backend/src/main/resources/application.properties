server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/repm_db
spring.datasource.username=root
spring.datasource.password=Kanishka@2005

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Serve frontend files directly from the frontend folder
spring.web.resources.static-locations=file:../frontend/

# Disable Thymeleaf (we use plain HTML + fetch API now)
spring.thymeleaf.enabled=false